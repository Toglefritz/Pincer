import 'package:flutter/material.dart';

import 'package:pincer/pincer_app.dart';

/// This is the entrypoint for the application. It performs app initialization and then runs the app.
void main() {
  runApp(const PincerApp());
}
